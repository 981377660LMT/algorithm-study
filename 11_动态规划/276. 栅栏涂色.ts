/**
 * @param {number} n
 * @param {number} k
 * @return {number}
 */
var numWays = function (n: number, k: number): number {}

console.log(numWays(3, 2))
// 输出：6
// 解释：所有的可能涂色方案如上图所示。
// 注意，全涂红或者全涂绿的方案属于无效方案，因为相邻的栅栏柱 最多连续两个 颜色相同。

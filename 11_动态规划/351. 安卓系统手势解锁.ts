/**
 * @param {number} m
 * @param {number} n
 * @return {number}
 * 那么请你统计一下有多少种 不同且有效的解锁模式 ，是 至少 需要经过 m 个点，但是 不超过 n 个点的。
 */
const numberOfPatterns = function (m: number, n: number): number {}

console.log(numberOfPatterns(1, 1))

// 9

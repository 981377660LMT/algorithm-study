/**
 * @param {string[]} equations
 * @return {boolean}
 * @description 并查集即可 看到!=时检查连通性
 */
const equationsPossible = function (equations: string[]): boolean {
  return true
}

console.log(equationsPossible(['a==b', 'b!=a']))

/**
 *
 * @param nums  1 <= k <= nums.length <= 2000
 * @param k
 * 返回数组中 要更改的最小元素数 ，以使所有长度为 k 的区间异或结果等于零。
 * @summary
 * 答案是k个一组重复的
 */
function minChanges(nums: number[], k: number): number {}

console.log(minChanges([1, 2, 0, 3, 0], 1))

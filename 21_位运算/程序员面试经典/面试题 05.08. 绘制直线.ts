// 绘制从点(x1, y)到点(x2, y)的水平线。
// 没看懂
function drawLine(length: number, w: number, x1: number, x2: number, y: number): number[] {
  const res = Array<number>(length).fill(0)

  return res
}

console.log(drawLine(1, 32, 30, 31, 0))
// 输出：[3]
// 在第0行的第30位到第31为画一条直线，屏幕表示为[0b000000000000000000000000000000011]

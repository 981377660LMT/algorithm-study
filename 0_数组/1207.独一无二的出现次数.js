// 数组在是哈希法中的经典应用
/**
 * @param {number[]} arr  1 <= arr.length <= 1000
 * @return {boolean}
   @summary counter+set
 */
var uniqueOccurrences = function (arr) {}
// 如果每个数的出现次数都是独一无二的，就返回 true；否则返回 false。

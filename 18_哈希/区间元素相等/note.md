`RangeAllSame` 区间相等/区间元素相等问题

### 1. 静态数组

前缀和/RLE

### 2. 动态数组（支持单点修改）

线段树

### 3. 动态数组（支持区间修改）

segmentSet

### 4. 随机化 / 哈希算法 (适用于特殊竞赛场景)

区间哈希

- **方案：**
  - **原理**：如果区间 `[l, r]` 全等于 $v$，那么该区间的哈希值应该等于 $v$ 重复 $r-l+1$ 次的哈希值。
  - **预处理**：维护数组的前缀哈希。
  - **缺点**：存在哈希冲突概率，且不支持高效修改（除非结合线段树维护哈希）。

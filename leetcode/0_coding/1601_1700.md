# LeetCode 1601-1700 面试速查

## 1601. Maximum Number of Achievable Transfer Requests

https://leetcode.cn/problems/maximum-number-of-achievable-transfer-requests/

枚举所有子集（n≤20），检查每个建筑净流入流出为0。

```python
def maximumRequests(self, n, requests):
    m = len(requests)
    res = 0
    for mask in range(1 << m):
        balance = [0] * n
        cnt = 0
        for i in range(m):
            if mask & (1 << i):
                balance[requests[i][0]] -= 1
                balance[requests[i][1]] += 1
                cnt += 1
        if all(b == 0 for b in balance):
            res = max(res, cnt)
    return res
```

## 1603. Design Parking System

https://leetcode.cn/problems/design-parking-system/

```python
class ParkingSystem:
    def __init__(self, big, medium, small):
        self.slots = [0, big, medium, small]
    def addCar(self, carType):
        if self.slots[carType] > 0:
            self.slots[carType] -= 1; return True
        return False
```

## 1604. Alert Using Same Key-Card Three or More Times in a One Hour Period

https://leetcode.cn/problems/alert-using-same-key-card-three-or-more-times-in-a-one-hour-period/

```python
from collections import defaultdict
def alertNames(self, keyName, keyTime):
    records = defaultdict(list)
    for name, time in zip(keyName, keyTime):
        h, m = map(int, time.split(':'))
        records[name].append(h * 60 + m)
    res = []
    for name, times in records.items():
        times.sort()
        for i in range(2, len(times)):
            if times[i] - times[i-2] <= 60:
                res.append(name); break
    return sorted(res)
```

## 1605. Find Valid Matrix Given Row and Column Sums

https://leetcode.cn/problems/find-valid-matrix-given-row-and-column-sums/

贪心：每个位置取 min(rowSum, colSum)。

```python
def restoreMatrix(self, rowSum, colSum):
    m, n = len(rowSum), len(colSum)
    mat = [[0]*n for _ in range(m)]
    for i in range(m):
        for j in range(n):
            v = min(rowSum[i], colSum[j])
            mat[i][j] = v
            rowSum[i] -= v; colSum[j] -= v
    return mat
```

## 1606. Find Servers That Handled Most Number of Requests

https://leetcode.cn/problems/find-servers-that-handled-most-number-of-requests/

SortedList + 最小堆。

思路兜底：维护空闲服务器有序列表和忙碌堆 (结束时间, 服务器编号)。每个请求从 i%k 开始找空闲服务器。

## 1608. Special Array With X Elements Greater Than or Equal To X

https://leetcode.cn/problems/special-array-with-x-elements-greater-than-or-equal-to-x/

```python
def specialArray(self, nums):
    nums.sort()
    n = len(nums)
    for x in range(n + 1):
        cnt = n - bisect.bisect_left(nums, x)
        if cnt == x: return x
    return -1
```

## 1609. Even Odd Tree

https://leetcode.cn/problems/even-odd-tree/

BFS 检查奇偶层条件。

```python
from collections import deque
def isEvenOddTree(self, root):
    q = deque([root]); level = 0
    while q:
        prev = float('-inf') if level % 2 == 0 else float('inf')
        for _ in range(len(q)):
            node = q.popleft()
            if level % 2 == 0:
                if node.val % 2 == 0 or node.val <= prev: return False
            else:
                if node.val % 2 == 1 or node.val >= prev: return False
            prev = node.val
            if node.left: q.append(node.left)
            if node.right: q.append(node.right)
        level += 1
    return True
```

## 1610. Maximum Number of Visible Points

https://leetcode.cn/problems/maximum-number-of-visible-points/

角度排序 + 滑动窗口。

思路兜底：将所有点转换为相对角度，排序后滑动窗口找 angle 范围内最多的点，加上与 location 重合的点。

## 1611. Minimum One Bit Operations to Make Integers Zero

https://leetcode.cn/problems/minimum-one-bit-operations-to-make-integers-zero/

格雷码规律：`f(n) = n ^ f(n >> 1)`。

```python
def minimumOneBitOperations(self, n):
    res = 0
    while n:
        res ^= n; n >>= 1
    return res
```

## 1614. Maximum Nesting Depth of the Parentheses

https://leetcode.cn/problems/maximum-nesting-depth-of-the-parentheses/

```python
def maxDepth(self, s):
    res = depth = 0
    for c in s:
        if c == '(': depth += 1; res = max(res, depth)
        elif c == ')': depth -= 1
    return res
```

## 1615. Maximal Network Rank

https://leetcode.cn/problems/maximal-network-rank/

```python
def maximalNetworkRank(self, n, roads):
    degree = [0] * n
    connected = set()
    for u, v in roads:
        degree[u] += 1; degree[v] += 1
        connected.add((u, v)); connected.add((v, u))
    res = 0
    for i in range(n):
        for j in range(i+1, n):
            rank = degree[i] + degree[j] - ((i, j) in connected)
            res = max(res, rank)
    return res
```

## 1616. Split Two Strings to Make Palindrome

https://leetcode.cn/problems/split-two-strings-to-make-palindrome/

双指针从两端向中间匹配。

```python
def checkPalindromeFormation(self, a, b):
    def check(s1, s2):
        i, j = 0, len(s1) - 1
        while i < j and s1[i] == s2[j]: i += 1; j -= 1
        sub1 = s1[i:j+1]; sub2 = s2[i:j+1]
        return sub1 == sub1[::-1] or sub2 == sub2[::-1]
    return check(a, b) or check(b, a)
```

## 1617. Count Subtrees With Max Distance Between Cities

https://leetcode.cn/problems/count-subtrees-with-max-distance-between-cities/

枚举子集 + BFS 求直径。

思路兜底：n ≤ 15，枚举所有 2^n 子集，检查是否连通，若连通求其直径。

## 1619. Mean of Array After Removing Some Elements

https://leetcode.cn/problems/mean-of-array-after-removing-some-elements/

```python
def trimMean(self, arr):
    arr.sort()
    n = len(arr)
    k = n // 20
    return sum(arr[k:n-k]) / (n - 2*k)
```

## 1620. Coordinate With Maximum Network Quality

https://leetcode.cn/problems/coordinate-with-maximum-network-quality/

暴力枚举坐标。

```python
def bestCoordinate(self, towers, radius):
    best = (0, 0); best_q = 0
    for x in range(51):
        for y in range(51):
            q = 0
            for tx, ty, qi in towers:
                d = ((x-tx)**2 + (y-ty)**2) ** 0.5
                if d <= radius:
                    q += int(qi / (1 + d))
            if q > best_q:
                best_q = q; best = (x, y)
    return list(best)
```

## 1621. Number of Sets of K Non-Overlapping Line Segments

https://leetcode.cn/problems/number-of-sets-of-k-non-overlapping-line-segments/

组合数学：`C(n+k-1, 2k)`.

```python
def numberOfSets(self, n, k):
    MOD = 10**9 + 7
    from math import comb
    return comb(n + k - 1, 2 * k) % MOD
```

## 1624. Largest Substring Between Two Equal Characters

https://leetcode.cn/problems/largest-substring-between-two-equal-characters/

```python
def maxLengthBetweenEqualCharacters(self, s):
    first = {}; res = -1
    for i, c in enumerate(s):
        if c in first: res = max(res, i - first[c] - 1)
        else: first[c] = i
    return res
```

## 1625. Lexicographically Smallest String After Applying Operations

https://leetcode.cn/problems/lexicographically-smallest-string-after-applying-operations/

BFS 枚举所有可达字符串。

```python
def findLexSmallestString(self, s, a, b):
    visited = set(); q = [s]; visited.add(s)
    res = s
    while q:
        cur = q.pop()
        res = min(res, cur)
        # 加 a
        added = ''.join(str((int(c) + a) % 10) if i % 2 else c for i, c in enumerate(cur))
        if added not in visited: visited.add(added); q.append(added)
        # 旋转 b
        rotated = cur[b:] + cur[:b]
        if rotated not in visited: visited.add(rotated); q.append(rotated)
    return res
```

## 1626. Best Team With No Conflicts

https://leetcode.cn/problems/best-team-with-no-conflicts/

按年龄排序后 DP（类似 LIS）。

```python
def bestTeamScore(self, scores, ages):
    pairs = sorted(zip(ages, scores))
    n = len(pairs)
    dp = [s for _, s in pairs]
    for i in range(n):
        for j in range(i):
            if pairs[j][1] <= pairs[i][1]:
                dp[i] = max(dp[i], dp[j] + pairs[i][1])
    return max(dp)
```

## 1627. Graph Connectivity With Threshold

https://leetcode.cn/problems/graph-connectivity-with-threshold/

并查集：枚举 threshold+1 到 n 的公因子。

```python
def areConnected(self, n, threshold, queries):
    parent = list(range(n + 1))
    def find(x):
        while parent[x] != x: parent[x] = parent[parent[x]]; x = parent[x]
        return x
    def union(a, b): parent[find(a)] = find(b)
    for z in range(threshold + 1, n + 1):
        for multiple in range(2 * z, n + 1, z):
            union(z, multiple)
    return [find(u) == find(v) for u, v in queries]
```

## 1629. Slowest Key

https://leetcode.cn/problems/slowest-key/

```python
def slowestKey(self, releaseTimes, keysPressed):
    res = keysPressed[0]; mx = releaseTimes[0]
    for i in range(1, len(keysPressed)):
        dur = releaseTimes[i] - releaseTimes[i-1]
        if dur > mx or (dur == mx and keysPressed[i] > res):
            mx = dur; res = keysPressed[i]
    return res
```

## 1630. Arithmetic Subarrays

https://leetcode.cn/problems/arithmetic-subarrays/

```python
def checkArithmeticSubarrays(self, nums, l, r):
    res = []
    for i, j in zip(l, r):
        sub = sorted(nums[i:j+1])
        res.append(len(set(sub[k+1]-sub[k] for k in range(len(sub)-1))) <= 1)
    return res
```

## 1631. Path With Minimum Effort

https://leetcode.cn/problems/path-with-minimum-effort/

Dijkstra / 二分 + BFS。

```python
import heapq
def minimumEffortPath(self, heights):
    m, n = len(heights), len(heights[0])
    dist = [[float('inf')]*n for _ in range(m)]
    dist[0][0] = 0
    h = [(0, 0, 0)]
    while h:
        d, r, c = heapq.heappop(h)
        if r == m-1 and c == n-1: return d
        if d > dist[r][c]: continue
        for dr, dc in [(-1,0),(1,0),(0,-1),(0,1)]:
            nr, nc = r+dr, c+dc
            if 0 <= nr < m and 0 <= nc < n:
                nd = max(d, abs(heights[nr][nc] - heights[r][c]))
                if nd < dist[nr][nc]:
                    dist[nr][nc] = nd
                    heapq.heappush(h, (nd, nr, nc))
    return 0
```

## 1632. Rank Transform of a Matrix

https://leetcode.cn/problems/rank-transform-of-a-matrix/

并查集 + 按值排序分配 rank。

思路兜底：将元素按值排序，同值的同行/同列用并查集合并，统一分配 rank = max(同组行列已有 rank) + 1。

## 1636. Sort Array by Increasing Frequency

https://leetcode.cn/problems/sort-array-by-increasing-frequency/

```python
from collections import Counter
def frequencySort(self, nums):
    cnt = Counter(nums)
    return sorted(nums, key=lambda x: (cnt[x], -x))
```

## 1637. Widest Vertical Area Between Two Points Containing No Points

https://leetcode.cn/problems/widest-vertical-area-between-two-points-containing-no-points/

```python
def maxWidthOfVerticalArea(self, points):
    xs = sorted(set(p[0] for p in points))
    return max(xs[i+1] - xs[i] for i in range(len(xs)-1))
```

## 1638. Count Substrings That Differ by One Character

https://leetcode.cn/problems/count-substrings-that-differ-by-one-character/

枚举不同的位置，向两边扩展。

```python
def countSubstrings(self, s, t):
    res = 0
    for i in range(len(s)):
        for j in range(len(t)):
            diff = 0
            k = 0
            while i+k < len(s) and j+k < len(t):
                if s[i+k] != t[j+k]: diff += 1
                if diff == 1: res += 1
                elif diff > 1: break
                k += 1
    return res
```

## 1639. Number of Ways to Form a Target String Given a Dictionary

https://leetcode.cn/problems/number-of-ways-to-form-a-target-string-given-a-dictionary/

DP：预计算每列每个字母的频率。

```python
def numWays(self, words, target):
    MOD = 10**9 + 7
    m = len(words[0]); n = len(target)
    freq = [[0]*26 for _ in range(m)]
    for w in words:
        for j, c in enumerate(w):
            freq[j][ord(c)-ord('a')] += 1
    dp = [0] * (n + 1); dp[0] = 1
    for j in range(m):
        for i in range(min(n, j+1), 0, -1):
            dp[i] = (dp[i] + dp[i-1] * freq[j][ord(target[i-1])-ord('a')]) % MOD
    return dp[n]
```

## 1640. Check Array Formation Through Concatenation

https://leetcode.cn/problems/check-array-formation-through-concatenation/

```python
def canFormArray(self, arr, pieces):
    mp = {p[0]: p for p in pieces}
    i = 0
    while i < len(arr):
        if arr[i] not in mp: return False
        p = mp[arr[i]]
        for x in p:
            if i >= len(arr) or arr[i] != x: return False
            i += 1
    return True
```

## 1641. Count Sorted Vowel Strings

https://leetcode.cn/problems/count-sorted-vowel-strings/

```python
def countVowelStrings(self, n):
    dp = [1] * 5
    for _ in range(n - 1):
        for i in range(3, -1, -1):
            dp[i] += dp[i+1]
    return sum(dp)
```

## 1642. Furthest Building You Can Reach

https://leetcode.cn/problems/furthest-building-you-can-reach/

最小堆：梯子用于最大的跳跃，砖块用于小的。

```python
import heapq
def furthestBuilding(self, heights, bricks, ladders):
    heap = []
    for i in range(len(heights) - 1):
        diff = heights[i+1] - heights[i]
        if diff <= 0: continue
        heapq.heappush(heap, diff)
        if len(heap) > ladders:
            bricks -= heapq.heappop(heap)
        if bricks < 0: return i
    return len(heights) - 1
```

## 1643. Kth Smallest Instructions

https://leetcode.cn/problems/kth-smallest-instructions/

组合数学：贪心从左到右确定每一步。

```python
from math import comb
def kthSmallestPath(self, destination, k):
    v, h = destination
    res = []
    for _ in range(h + v):
        if h > 0:
            # 选 H：剩余排列数
            cnt = comb(h - 1 + v, v)
            if k <= cnt:
                res.append('H'); h -= 1
            else:
                k -= cnt
                res.append('V'); v -= 1
        else:
            res.append('V'); v -= 1
    return ''.join(res)
```

## 1646. Get Maximum in Generated Array

https://leetcode.cn/problems/get-maximum-in-generated-array/

```python
def getMaximumGenerated(self, n):
    if n <= 1: return n
    arr = [0] * (n + 1); arr[1] = 1
    for i in range(2, n + 1):
        arr[i] = arr[i//2] + (arr[i//2+1] if i % 2 else 0)
    return max(arr)
```

## 1647. Minimum Deletions to Make Character Frequencies Unique

https://leetcode.cn/problems/minimum-deletions-to-make-character-frequencies-unique/

贪心：频率降序排列，确保不重复。

```python
from collections import Counter
def minDeletions(self, s):
    freqs = sorted(Counter(s).values(), reverse=True)
    res = 0; max_allowed = freqs[0]
    for f in freqs:
        if f > max_allowed:
            res += f - max_allowed
            f = max_allowed
        max_allowed = max(0, f - 1)
    return res
```

## 1648. Sell Diminishing-Valued Colored Balls

https://leetcode.cn/problems/sell-diminishing-valued-colored-balls/

排序 + 贪心/二分。

思路兜底：排序后从最大开始卖，每次将最大的降到次大的水平，计算卖出数量和收入。

## 1649. Create Sorted Array through Instructions

https://leetcode.cn/problems/create-sorted-array-through-instructions/

BIT / 归并排序。

思路兜底：用树状数组维护已插入元素，每次插入前查询 < 和 > 的个数，取 min 为代价。

## 1652. Defuse the Bomb

https://leetcode.cn/problems/defuse-the-bomb/

```python
def decrypt(self, code, k):
    n = len(code)
    if k == 0: return [0] * n
    res = [0] * n
    for i in range(n):
        if k > 0: res[i] = sum(code[(i+j) % n] for j in range(1, k+1))
        else: res[i] = sum(code[(i+j) % n] for j in range(k, 0))
    return res
```

## 1653. Minimum Deletions to Make String Balanced

https://leetcode.cn/problems/minimum-deletions-to-make-string-balanced/

DP：维护遇到的 'b' 数量。

```python
def minimumDeletions(self, s):
    b_count = res = 0
    for c in s:
        if c == 'b': b_count += 1
        else: res = min(res + 1, b_count)
    return res
```

## 1654. Minimum Jumps to Reach Home

https://leetcode.cn/problems/minimum-jumps-to-reach-home/

BFS 状态 = (位置, 方向)。

```python
from collections import deque
def minimumJumps(self, forbidden, a, b, x):
    forbidden = set(forbidden)
    limit = max(max(forbidden, default=0), x) + a + b
    visited = set()
    q = deque([(0, False, 0)])  # pos, went_back, steps
    visited.add((0, False))
    while q:
        pos, back, steps = q.popleft()
        if pos == x: return steps
        # 前进
        nf = pos + a
        if nf <= limit and nf not in forbidden and (nf, False) not in visited:
            visited.add((nf, False)); q.append((nf, False, steps + 1))
        # 后退（只有上一步不是后退时才能后退）
        if not back:
            nb = pos - b
            if nb >= 0 and nb not in forbidden and (nb, True) not in visited:
                visited.add((nb, True)); q.append((nb, True, steps + 1))
    return -1
```

## 1655. Distribute Repeating Integers

https://leetcode.cn/problems/distribute-repeating-integers/

状压DP：订单子集是否能被某个频率满足。

思路兜底：统计值的频率，`dp[mask]` = 前 i 个频率能满足 mask 子集的订单，枚举子集转移。

## 1656. Design an Ordered Stream

https://leetcode.cn/problems/design-an-ordered-stream/

```python
class OrderedStream:
    def __init__(self, n):
        self.data = [None] * (n + 1); self.ptr = 1
    def insert(self, idKey, value):
        self.data[idKey] = value
        res = []
        while self.ptr < len(self.data) and self.data[self.ptr]:
            res.append(self.data[self.ptr]); self.ptr += 1
        return res
```

## 1657. Determine if Two Strings Are Close

https://leetcode.cn/problems/determine-if-two-strings-are-close/

```python
from collections import Counter
def closeStrings(self, word1, word2):
    c1, c2 = Counter(word1), Counter(word2)
    return set(c1.keys()) == set(c2.keys()) and sorted(c1.values()) == sorted(c2.values())
```

## 1658. Minimum Operations to Reduce X to Zero

https://leetcode.cn/problems/minimum-operations-to-reduce-x-to-zero/

转化为找和为 `sum - x` 的最长子数组。

```python
def minOperations(self, nums, x):
    target = sum(nums) - x
    if target < 0: return -1
    if target == 0: return len(nums)
    left = cur = 0; max_len = -1
    for right in range(len(nums)):
        cur += nums[right]
        while cur > target: cur -= nums[left]; left += 1
        if cur == target: max_len = max(max_len, right - left + 1)
    return len(nums) - max_len if max_len != -1 else -1
```

## 1659. Maximize Grid Happiness

https://leetcode.cn/problems/maximize-grid-happiness/

状压DP，记录每行的人员分配状态。

思路兜底：m\*n ≤ 25，按行逐格摆放，三进制状压记录当前行状态，考虑相邻格的幸福度变化。

## 1662. Check If Two String Arrays are Equivalent

https://leetcode.cn/problems/check-if-two-string-arrays-are-equivalent/

```python
def arrayStringsAreEqual(self, word1, word2):
    return ''.join(word1) == ''.join(word2)
```

## 1663. Smallest String With A Given Numeric Value

https://leetcode.cn/problems/smallest-string-with-a-given-numeric-value/

贪心从后往前填最大值。

```python
def getSmallestString(self, n, k):
    res = ['a'] * n; k -= n
    for i in range(n-1, -1, -1):
        add = min(25, k)
        res[i] = chr(ord('a') + add)
        k -= add
        if k == 0: break
    return ''.join(res)
```

## 1664. Ways to Make a Fair Array

https://leetcode.cn/problems/ways-to-make-a-fair-array/

前缀和：删除 i 后奇偶位的和交换。

```python
def waysToMakeFair(self, nums):
    n = len(nums)
    even_suffix = [0] * (n + 1); odd_suffix = [0] * (n + 1)
    for i in range(n-1, -1, -1):
        even_suffix[i] = even_suffix[i+1] + (nums[i] if i % 2 == 0 else 0)
        odd_suffix[i] = odd_suffix[i+1] + (nums[i] if i % 2 == 1 else 0)
    res = 0; even_pre = odd_pre = 0
    for i in range(n):
        # 删除 i 后：前面不变，后面奇偶交换
        new_even = even_pre + odd_suffix[i+1]
        new_odd = odd_pre + even_suffix[i+1]
        if new_even == new_odd: res += 1
        if i % 2 == 0: even_pre += nums[i]
        else: odd_pre += nums[i]
    return res
```

## 1668. Maximum Repeating Substring

https://leetcode.cn/problems/maximum-repeating-substring/

```python
def maxRepeating(self, sequence, word):
    k = 0
    while word * (k + 1) in sequence: k += 1
    return k
```

## 1669. Merge In Between Linked Lists

https://leetcode.cn/problems/merge-in-between-linked-lists/

```python
def mergeInBetween(self, list1, a, b, list2):
    cur = list1
    for _ in range(a - 1): cur = cur.next
    start = cur
    for _ in range(b - a + 2): cur = cur.next
    start.next = list2
    tail = list2
    while tail.next: tail = tail.next
    tail.next = cur
    return list1
```

## 1670. Design Front Middle Back Queue

https://leetcode.cn/problems/design-front-middle-back-queue/

两个双端队列维护前后半部分。

```python
from collections import deque
class FrontMiddleBackQueue:
    def __init__(self):
        self.front = deque(); self.back = deque()
    def _balance(self):
        while len(self.front) > len(self.back):
            self.back.appendleft(self.front.pop())
        while len(self.front) < len(self.back) - 1:
            self.front.append(self.back.popleft())
    def pushFront(self, val): self.front.appendleft(val); self._balance()
    def pushMiddle(self, val): self.front.append(val); self._balance()
    def pushBack(self, val): self.back.append(val); self._balance()
    def popFront(self):
        if not self.front and not self.back: return -1
        if self.front: val = self.front.popleft()
        else: val = self.back.popleft()
        self._balance(); return val
    def popMiddle(self):
        if not self.back: return -1
        if len(self.front) == len(self.back): val = self.front.pop()
        else: val = self.back.popleft()
        self._balance(); return val
    def popBack(self):
        if not self.back: return -1
        val = self.back.pop(); self._balance(); return val
```

## 1672. Richest Customer Wealth

https://leetcode.cn/problems/richest-customer-wealth/

```python
def maximumWealth(self, accounts):
    return max(sum(row) for row in accounts)
```

## 1673. Find the Most Competitive Subsequence

https://leetcode.cn/problems/find-the-most-competitive-subsequence/

单调栈。

```python
def mostCompetitive(self, nums, k):
    stack = []
    n = len(nums)
    for i, x in enumerate(nums):
        while stack and stack[-1] > x and len(stack) + n - i > k:
            stack.pop()
        if len(stack) < k: stack.append(x)
    return stack
```

## 1674. Minimum Moves to Make Array Complementary

https://leetcode.cn/problems/minimum-moves-to-make-array-complementary/

差分数组分析每对的贡献。

思路兜底：对每对 (a, b)，不同 T 值需要 0/1/2 次操作，用差分数组统计。

## 1675. Minimize Deviation in Array

https://leetcode.cn/problems/minimize-deviation-in-array/

最大堆：奇数\*2，然后不断将最大值/2。

```python
import heapq
def minimumDeviation(self, nums):
    h = []
    mn = float('inf')
    for x in nums:
        if x % 2: x *= 2
        heapq.heappush(h, -x)
        mn = min(mn, x)
    res = -h[0] - mn
    while h[0] % 2 == 0:
        mx = -heapq.heappop(h)
        mx //= 2; mn = min(mn, mx)
        heapq.heappush(h, -mx)
        res = min(res, -h[0] - mn)
    return res
```

## 1678. Goal Parser Interpretation

https://leetcode.cn/problems/goal-parser-interpretation/

```python
def interpret(self, command):
    return command.replace('()', 'o').replace('(al)', 'al')
```

## 1679. Max Number of K-Sum Pairs

https://leetcode.cn/problems/max-number-of-k-sum-pairs/

```python
from collections import Counter
def maxOperations(self, nums, k):
    cnt = Counter(nums); res = 0
    for x in list(cnt):
        comp = k - x
        if comp == x: res += cnt[x] // 2; cnt[x] %= 2
        elif comp in cnt:
            pairs = min(cnt[x], cnt[comp])
            res += pairs; cnt[x] -= pairs; cnt[comp] -= pairs
    return res
```

## 1680. Concatenation of Consecutive Binary Numbers

https://leetcode.cn/problems/concatenation-of-consecutive-binary-numbers/

```python
def concatenatedBinary(self, n):
    MOD = 10**9 + 7
    res = 0
    for i in range(1, n + 1):
        res = ((res << i.bit_length()) | i) % MOD
    return res
```

## 1684. Count the Number of Consistent Strings

https://leetcode.cn/problems/count-the-number-of-consistent-strings/

```python
def countConsistentStrings(self, allowed, words):
    s = set(allowed)
    return sum(all(c in s for c in w) for w in words)
```

## 1685. Sum of Absolute Differences in a Sorted Array

https://leetcode.cn/problems/sum-of-absolute-differences-in-a-sorted-array/

```python
def getSumAbsoluteDifferences(self, nums):
    n = len(nums); total = sum(nums)
    prefix = 0; res = []
    for i, x in enumerate(nums):
        left_sum = prefix; right_sum = total - prefix - x
        res.append(x * i - left_sum + right_sum - x * (n - i - 1))
        prefix += x
    return res
```

## 1686. Stone Game VI

https://leetcode.cn/problems/stone-game-vi/

按 `a[i] + b[i]` 降序排序，交替选择。

```python
def stoneGameVI(self, aliceValues, bobValues):
    pairs = sorted(zip([a+b for a,b in zip(aliceValues, bobValues)], aliceValues, bobValues), reverse=True)
    alice = sum(a for _, a, _ in pairs[::2])
    bob = sum(b for _, _, b in pairs[1::2])
    return 0 if alice == bob else (1 if alice > bob else -1)
```

## 1688. Count of Matches in Tournament

https://leetcode.cn/problems/count-of-matches-in-tournament/

```python
def numberOfMatches(self, n):
    return n - 1
```

## 1689. Partitioning Into Minimum Number Of Deci-Binary Numbers

https://leetcode.cn/problems/partitioning-into-minimum-number-of-deci-binary-numbers/

答案 = 最大数字位。

```python
def minPartitions(self, n):
    return int(max(n))
```

## 1690. Stone Game VII

https://leetcode.cn/problems/stone-game-vii/

区间 DP。

```python
def stoneGameVII(self, stones):
    n = len(stones)
    prefix = [0] * (n + 1)
    for i in range(n): prefix[i+1] = prefix[i] + stones[i]
    dp = [[0]*n for _ in range(n)]
    for length in range(2, n+1):
        for i in range(n-length+1):
            j = i + length - 1
            s = prefix[j+1] - prefix[i]
            dp[i][j] = max(s - stones[i] - dp[i+1][j], s - stones[j] - dp[i][j-1])
    return dp[0][n-1]
```

## 1694. Reformat Phone Number

https://leetcode.cn/problems/reformat-phone-number/

```python
def reformatNumber(self, number):
    digits = number.replace(' ', '').replace('-', '')
    groups = []; i = 0
    while i < len(digits):
        rem = len(digits) - i
        if rem <= 4:
            if rem == 4: groups.extend([digits[i:i+2], digits[i+2:i+4]]); i += 4
            else: groups.append(digits[i:]); i = len(digits)
        else:
            groups.append(digits[i:i+3]); i += 3
    return '-'.join(groups)
```

## 1695. Maximum Erasure Value

https://leetcode.cn/problems/maximum-erasure-value/

滑动窗口。

```python
def maximumUniqueSubarray(self, nums):
    seen = set(); left = cur = res = 0
    for x in nums:
        while x in seen:
            seen.remove(nums[left]); cur -= nums[left]; left += 1
        seen.add(x); cur += x; res = max(res, cur)
    return res
```

## 1696. Jump Game VI

https://leetcode.cn/problems/jump-game-vi/

单调递减队列优化 DP。

```python
from collections import deque
def maxResult(self, nums, k):
    n = len(nums)
    dp = [0] * n; dp[0] = nums[0]
    dq = deque([0])
    for i in range(1, n):
        while dq and dq[0] < i - k: dq.popleft()
        dp[i] = dp[dq[0]] + nums[i]
        while dq and dp[dq[-1]] <= dp[i]: dq.pop()
        dq.append(i)
    return dp[-1]
```

## 1697. Checking Existence of Edge Length Limited Paths

https://leetcode.cn/problems/checking-existence-of-edge-length-limited-paths/

离线查询：按 limit 排序，逐步加边并查集。

```python
def distanceLimitedPathsExist(self, n, edgeList, queries):
    parent = list(range(n))
    def find(x):
        while parent[x] != x: parent[x] = parent[parent[x]]; x = parent[x]
        return x
    def union(a, b): parent[find(a)] = find(b)
    edgeList.sort(key=lambda x: x[2])
    indexed_queries = sorted(enumerate(queries), key=lambda x: x[1][2])
    res = [False] * len(queries)
    ei = 0
    for qi, (u, v, limit) in indexed_queries:
        while ei < len(edgeList) and edgeList[ei][2] < limit:
            union(edgeList[ei][0], edgeList[ei][1]); ei += 1
        res[qi] = find(u) == find(v)
    return res
```

## 1700. Number of Students Unable to Eat Lunch

https://leetcode.cn/problems/number-of-students-unable-to-eat-lunch/

```python
from collections import Counter
def countStudents(self, students, sandwiches):
    cnt = Counter(students)
    for s in sandwiches:
        if cnt[s] > 0: cnt[s] -= 1
        else: return cnt[0] + cnt[1]
    return 0
```

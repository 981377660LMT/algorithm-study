/**
 * @param {number[][]} nums  你有 k 个 非递减排列 的整数列表
 * @return {number[]}
 * 找到一个 最小 区间，使得 k 个列表中的每个列表至少有一个数包含在其中。
 */
var smallestRange = function (nums: number[][]): number[] {}

console.log(
  smallestRange([
    [4, 10, 15, 24, 26],
    [0, 9, 12, 20],
    [5, 18, 22, 30],
  ])
)

/**
 * 静态查询区间的莫队算法
 *
 * @param data 原始数据
 */
function useMoAlgo<T = number, Q = number>(data: T[]) {
  function addQuery(params: type) {}
  return {
    addQuery,
  }
}

export { useMoAlgo }

# 给定长度为n的数组，每个元素代表一个木头的长度，木头可以任意截断，
# 从这堆木头中截出至少k个相同长度为m的木块。已知k，求max(m)。


# 二分答案
from typing import List

# easy
def cut(wood: List[int], k: int) -> int:
    pass


# 从这堆木头中截出至少k个相同长度为m的木块。已知k，求max(m)。
print(cut([4, 7, 2, 10, 5], 5))


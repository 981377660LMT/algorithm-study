import { BinaryTree } from '../Tree'

/**
 * @param {BinaryTree} root
 * @param {number} key
 * @return {BinaryTree}
 * @description 先找节点再判断左右情况
 */
const deleteNode = function (root: BinaryTree, key: number): BinaryTree {}

export {}

// Generates a random string with the specified length.
console.log(randomAlphaNumeric(5)) // '0afad'

function randomAlphaNumeric(length: number): any {
  const stringBuilder: string[] = []
}
Math.hypot()

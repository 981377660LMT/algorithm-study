# 成熟工作系统中落地harness

1. "给文档、让 Agent 读、再去改代码"的直觉，针对新的独立项目已经足够。在业务里挑一个开发和测试能闭环的链路，让 AI 完全自主实现，也不再有挑战。但问题一旦扩大为在成熟系统里稳定自主地迭代功能，结构性的阻力就会出现，ToB 项目则更甚。

2. 隐性知识(上下文根本无法被文档化)

3. 验收标准：
   实践是检验真理的唯一标准，判断 Harness 是否跑通，就看：一个 Agent 进来，能不能在不问人的情况下，找到规则、找到代码、完成任务、交付结果。
   交付的结果应该是端到端的。

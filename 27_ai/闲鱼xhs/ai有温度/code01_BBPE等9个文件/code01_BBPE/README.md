> 小红书 @AI有温度

### 运行train.py 

1. 训练后会生成vocab.json和merge.txt：
    - vocab.json：
        - 这个文件包含了由 BBPE 算法生成的词汇表（vocabulary），其中每个词汇都是一个字节序列或者子词（subword）。
        - 词汇表中的每个词汇都与一个整数 ID 相关联，用于在编码和解码过程中进行索引和映射。
        - 通常，vocab.json 中的词汇按照其频率（或者其他指标）进行排序，以便在 BBPE 算法中进行合并时能够优先选择频繁出现的子词。
    - merge.txt：
        - 这个文件包含了 BBPE 算法在训练过程中执行的所有合并操作。
        - 每行都代表一个合并操作，其格式通常是两个子词以及它们合并后的结果，以空格或其他分隔符分开。
        - 例如，一行可能是 ababab，表示在某一次迭代中，BBPE 算法将子词 a 和 b 合并成新的子词 ab。
2. BBPE的实现过程在bbpe.py文件，建议大家debug学习
3. 训练数据可以换成任意txt，都会根据当前txt内容进行词表生成

### 运行test.py 

- 可以选择自己训练好的vocab.json和merge.txt
- 也为大家准备了llama和qwen的词表，可以进行对比
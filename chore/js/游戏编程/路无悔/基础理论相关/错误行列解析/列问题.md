1. 我写的代码是

```code
eer
return Math.floor(Math.random() * 500) + 1
```

但是执行的是

```code
(function anonymous(context,superCtx,shim
) {
with(shim) { with(superCtx) { with(context) { return (() => { eer
return Math.floor(Math.random() * 500) + 1;;
 })(); } } }
})
```

因此解析出来报错列不对，是 eer is not defined (行 1, 列 63)

2. 当我写的代码是

```code
//
eer
return Math.floor(Math.random() * 500) + 1
```

此时执行的是

```code
(function anonymous(context,superCtx,shim
) {
with(shim) { with(superCtx) { with(context) { return (() => { //
eer
return Math.floor(Math.random() * 500) + 1;;
 })(); } } }
})
```

报错列又是对的

根据最佳实践，如何解决这个问题
